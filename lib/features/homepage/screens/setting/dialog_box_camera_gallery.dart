import 'package:flutter/material.dart';

class DialogBoxCameraGallery extends StatelessWidget{
  const DialogBoxCameraGallery({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      
    );

  }
}